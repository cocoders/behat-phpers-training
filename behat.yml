default:
    suites:
        model:
            contexts: [ FeatureContext ]
        online:
            contexts:
                - OnlineFeatureContext
            filters:  { tags: critical }
    extensions:
        Behat\MinkExtension:
            base_url: http://application:8000
            sessions:
                main:
                    goutte: ~
